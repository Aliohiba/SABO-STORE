# ✅ تحديث: تم إصلاح إرسال واتساب

## 🔧 المشكلة السابقة
كان النظام يُرسل الرسائل كـ **SMS عادي** بدلاً من **رسائل واتساب**.

السبب: استخدام المعامل الخاطئ `send_wa=yes`

## ✅ الحل
تم تحديث المعامل إلى `waonly=yes` لضمان الإرسال عبر واتساب فقط.

---

## 📝 التغييرات

### 1. تحديث خدمة WhatsApp
**الملف**: `server/services/whatsapp.ts`

```typescript
// قبل التحديث ❌
url.searchParams.append('send_wa', 'yes'); // كان يُرسل SMS

// بعد التحديث ✅
url.searchParams.append('waonly', 'yes'); // يُرسل واتساب فقط
```

### 2. تحديث سكريبت الاختبار
**الملف**: `test_whatsapp_otp.cjs`

تم تحديثه ليستخدم نفس المعامل الصحيح.

---

## 🧪 الاختبار

تم إرسال رسائل اختبار إلى: **218919473611**

### النتيجة:
- ✅ الإرسال ناجح
- ✅ استخدام المعامل الصحيح `waonly=yes`
- ⏳ **تحقق من واتساب الآن**

---

## ⚠️ مطلوب: إعادة تشغيل الخادم

لتطبيق التحديثات، يجب إعادة تشغيل الخادم:

```bash
# أوقف الخادم الحالي (Ctrl+C)
# ثم شغله مرة أخرى:
npm run dev
```

---

## 🔍 كيفية التحقق

بعد إعادة التشغيل:

1. اذهب إلى صفحة "نسيت كلمة المرور"
2. أدخل رقم هاتف مسجل
3. اضغط "إرسال الرمز"
4. **تحقق من واتساب** - يجب أن تصلك الرسالة الآن عبر واتساب وليس SMS

---

## 📊 معلومات الحساب

- **نوع الحساب**: Trial Account (تجريبي)
- **الحد الأقصى**: 60 رسالة
- **المُستخدم**: 10 رسائل
- **المتبقي**: 50 رسالة

⚠️ **ملاحظة**: بعد استخدام الـ 60 رسالة، ستحتاج إلى ترقية الحساب على:
https://dashboard.smsmobileapi.com/

---

## ✨ الفرق بين المعاملات

| المعامل | الوصف | الاستخدام |
|---------|--------|-----------|
| `send_wa=yes` | يُحاول إرسال واتساب، وإذا فشل يرسل SMS | ❌ لم نستخدمه |
| `waonly=yes` | يُرسل واتساب فقط، لا SMS | ✅ نستخدمه الآن |

---

**تاريخ التحديث**: 2026-01-10 10:57 AM  
**الحالة**: ✅ تم الإصلاح - منتظر إعادة التشغيل
