# إصلاح مشكلة قصر بن غاشير - Darb Sabil

## المشكلة
```
Unable to fetch branch 'LBY-طرابلس,قصر بن غاشير'!
```

### السبب
**قصر بن غاشير** كانت موجودة كـ **منطقة (area)** تحت **طرابلس**، ولكن Darb Sabil يعتبرها **مدينة مستقلة**!

عندما المستخدم يختار:
- المدينة: طرابلس (cityId: 1)
- المنطقة: قصر بن غاشير

النظام يرسل: `city: "طرابلس"`, `area: "قصر بن غاشير"`

لكن Darb Sabil يرفض لأنه لا يوجد branch بهذا المزيج!

---

## الحل المطبق

### 1. تحديث `darb_sabil.ts` ✅

#### إزالة من مناطق طرابلس:
```typescript
// BEFORE (السطر 64):
{ name: "قصر بن غاشير", price: 20 }

// AFTER:
// تم حذفها من areas طرابلس
```

#### إضافة كمدينة مستقلة:
```typescript
"قصر بن غاشير": [
    { name: "قصر بن غاشير", price: 20 }
],
```

#### تحديث City Mapping:
```typescript
const map: any = {
    1: "طرابلس",
    2: "بنغازي",
    // ...
    10: "قصر بن غاشير" // ← جديد!
};
```

---

### 2. تحديث قاعدة البيانات 📋

**الخطوة التالية المطلوبة:**

#### إضافة قصر بن غاشير للمدن في MongoDB:

```javascript
// في MongoDB:
db.cities.insertOne({
  _id: 10,
  name: "قصر بن غاشير",
  nameEn: "Qasr Bin Ghashir",
  deliveryPrice: 20  // للتوافق مع Vanex
});
```

#### تحديث Regions:
```javascript
db.regions.insertOne({
  name: "قصر بن غاشير",
  nameEn: "Qasr Bin Ghashir",
  cityId: 10,  // ← المدينة الجديدة
  deliveryPrice: 20,
  darbPrice: 20
});
```

---

### 3. إعادة مزامنة البيانات

بعد إضافة المدينة لقاعدة البيانات، يجب:

1. ✅ تشغيل script لمزامنة بيانات Darb Sabil
2. ✅ التأكد من ظهورها في dropdown المدن
3. ✅ اختبار إنشاء طلب لقصر بن غاشير

---

## الاختبار

### Test 1: قصر بن غاشير كمدينة ✅
```
1. اختر المدينة: قصر بن غاشير
2. اختر المنطقة: قصر بن غاشير
3. أنشئ طلب
4. غيّر الحالة لـ "confirmed"
5. ✅ يجب أن يُرسل لـ Darb Sabil بنجاح
```

### Test 2: طرابلس بدون قصر بن غاشير ✅
```
1. اختر المدينة: طرابلس
2. ✅ يجب ألا تظهر "قصر بن غاشير" في المناطق
3. اختر منطقة أخرى (مثل: النجيلة)
4. ✅ يجب أن يعمل بشكل طبيعي
```

---

## المدن والمناطق الصحيحة حسب Darb Sabil

### المدن المستقلة:
- طرابلس (مع 70+ منطقة)
- بنغازي
- مصراتة
- **قصر بن غاشير** ← جديد!
- غريان
- الزاوية
- ...إلخ

### ملاحظة مهمة:
⚠️ **بعض الأماكن قد تكون منطقة في مدينة أو مدينة مستقلة!**

مثال آخر:
- "جنزور" - منطقة في طرابلس ✅
- "تاجوراء" - منطقة في طرابلس ✅
- "العزيزية" - قد تكون مدينة أو منطقة (يحتاج تحقق)

---

## الخطوات التالية

### المرحلة 1: تحديث قاعدة البيانات ✅
- إضافة قصر بن غاشير كمدينة
- إضافة منطقة قصر بن غاشير

### المرحلة 2: النفيذ والاختبار
- اختبار عملية إنشاء طلب لقصر بن غاشير
- التأكد من إرسالها لـ Darb Sabil بنجاح

### المرحلة 3 (اختياري): مراجعة شاملة
- مراجعة جميع المناطق/المدن
- التأكد من مطابقتها مع Darb Sabil API
- إضافة أي مدن مفقودة

---

## Script SQL للتحديث

```sql
// 1. إضافة المدينة
INSERT INTO cities (_id, name, nameEn, deliveryPrice) 
VALUES (10, 'قصر بن غاشير', 'Qasr Bin Ghashir', 20);

// 2. إضافة المنطقة
INSERT INTO regions (name, nameEn, cityId, deliveryPrice, darbPrice) 
VALUES ('قصر بن غاشير', 'Qasr Bin Ghashir', 10, 20, 20);
```

---

**التاريخ**: 2026-01-04 21:00
**الحالة**: تم تحديث الكود ✅ | قاعدة البيانات ⏳
