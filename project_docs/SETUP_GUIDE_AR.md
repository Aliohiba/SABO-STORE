# 🛠️ دليل تشغيل المشروع على جهاز جديد

إليك الخطوات التفصيلية لنقل وتشغيل متجر "SABO STORE" على جهاز كمبيوتر آخر.

## 1. تثبيت البرامج الأساسية (مرة واحدة فقط)
قبل البدء، تأكد من تثبيت البرنامجين التاليين على الجهاز الجديد:
1.  **Node.js**: حمل النسخة "LTS" من [الموقع الرسمي](https://nodejs.org/).
2.  **Git**: حمل وثبت البرنامج من [الموقع الرسمي](https://git-scm.com/).

---

## 2. تحميل كود المشروع
لديك خياران:

### الخيار أ: التحميل من GitHub (الأفضل)
افتح الـ Terminal (أو CMD) في المجلد الذي تريد وضع المشروع فيه، واكتب:
```bash
git clone https://github.com/Aliohiba/SABO-STORE.git
cd SABO-STORE
```

### الخيار ب: النقل عبر الفلاش (USB)
إذا نسخت مجلد المشروع بالكامل، فقط ضعه في المكان الذي تريده وافتحه.

---

## 3. استعادة الملفات السرية (مهم جداً) 🔑
ملف `.env` لا يتم تحميله من GitHub لأسباب أمنية.
1.  احضر ملف `.env` الذي قمت بنسخه احتياطياً (من المجلد `test` أو الفلاش).
2.  ضعه داخل مجلد المشروع `SABO-STORE` (بجانب الملفات `package.json` و `vite.config.ts`).

---

## 4. تشغيل المشروع 🚀
الآن، افتح الـ Terminal داخل مجلد المشروع ونفذ الأمرين التاليين بالترتيب:

**أولاً: تحميل المكتبات (يستغرق وقتاً قليلاً)**
```bash
npm install
```

**ثانياً: تشغيل الموقع**
```bash
npm run dev
```

بعدها سيظهر لك رابط (مثل `http://localhost:5000`)، اضغط عليه لفتح الموقع.
